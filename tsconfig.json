{
  "compilerOptions": {
    // ...other options
    "baseUrl": ".", // Ensure this is present
    "paths": {
      "@/*": ["./*"] // This enables absolute imports from the project root
    }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}
